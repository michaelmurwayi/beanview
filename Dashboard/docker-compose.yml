version: "3.8"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: dashboard:latest   # optional: gives the built image a tag
    ports:
      - "443:443"
    environment:
      - NODE_ENV=production
    restart: always
